# ─── Environment files (actual values — never commit) ─────────────────────────
.env
.env.production
.env.local
.env.*.local
ocs-api/.env
ocs-api/.env.production
ocs-web/.env.production
ocs-web/.env.local
# .env.development files are committed (no secrets, just localhost defaults)
# .env.example files are committed (templates)

# ─── Java / Maven ─────────────────────────────────────────────────────────────
ocs-api/target/
ocs-api/.mvn/
*.class
*.jar
*.war

# ─── Node / Vite ──────────────────────────────────────────────────────────────
ocs-web/node_modules/
ocs-web/dist/
ocs-web/.vite/

# ─── C++ / CMake ──────────────────────────────────────────────────────────────
ocs-calculator/build/
*.o
*.a
*.so
*.out

# ─── IDE / OS ─────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

#old program
old-program/*
