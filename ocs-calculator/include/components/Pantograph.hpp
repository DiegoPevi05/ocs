#pragma once

namespace catenary {
namespace components {

// Optional: Placeholder struct if more fields are needed later
struct Pantograph {
    // Currently empty in Cantilever.server.ts, passing it to constructor is sufficient
};

} // namespace components
} // namespace catenary
